name: Docker Build and Push

on:
  push:
    branches: [ '**' ]
    tags: [ 'v*' ]
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to build and push (e.g. v1.2.3)'
        required: true
        type: string

env:
  # TODO: Change to your Gitea instance domain (e.g., git.example.com)
  REGISTRY: registry.vicedomini.ltd
  IMAGE_NAME: vicedomini-softworks/viceregistry

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag || github.ref }}

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Gitea Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_TOKEN }}

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=branch
            type=ref,event=tag
            type=raw,value=${{ inputs.tag }},enable=${{ github.event_name == 'workflow_dispatch' }}
            type=sha,format=short

      - name: Build image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          load: true
          tags: viceregistry:local-build
          labels: ${{ steps.meta.outputs.labels }}

      - name: Push tags sequentially
        run: |
          while IFS= read -r tag; do
            [ -z "$tag" ] && continue
            docker tag viceregistry:local-build "$tag"
            docker push "$tag"
          done <<< "${{ steps.meta.outputs.tags }}"
